import { Link } from 'react-router-dom';
import React, { FC } from 'react';

const Dashboard: FC = () => {
    return (
        <>
            <div>Dashboard</div>
        </>
    );
};

export default Dashboard;
