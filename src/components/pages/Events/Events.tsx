import { Link } from 'react-router-dom';
import React, { FC } from 'react';

const EventsPage: FC = () => {
    return (
        <>
            <div>Events Page</div>
        </>
    );
};

export default EventsPage;
